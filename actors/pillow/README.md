# Pillow actor

Runtime scene, script, sprites, portraits, effects, and source-art references for the player character.

Runtime PNG files belong under `sprites/`, `portraits/`, and `effects/`. Large unsliced references belong under `source_art/`, which is excluded from Godot import with `.gdignore`.
